# vercel-skills-code-reviewer — maintenance note

Clean first patch on agents/code-reviewer.md (nothing else targets it), so apply.sh reports it as
"upstream unchanged" until OMC itself edits code-reviewer.md. baseline.md is pristine (copied from the
unpatched active cache). Block appended at EOF. Do NOT --update-baseline unless upstream changes the file.
