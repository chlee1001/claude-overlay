# vercel-skills-omc-reference — maintenance note

Clean first patch on skill-bodies/omc-reference/SKILL.md. Delivers the "when → which vercel skill"
routing table, including the light architect/designer pointers. architect/designer are advisory lanes
(a missed pointer only softens advice, never ships bad code), so they route via this omc-reference table
rather than their own agent patches — only executor + code-reviewer get explicit patches. baseline.md is pristine from the unpatched
active cache. Block appended at EOF. NOTE: marketplace root has no skill-bodies/, so this target exists
only in the active plugin cache — apply.sh will find it there (MISSING on the marketplace root is normal).
Do NOT --update-baseline unless upstream changes omc-reference.
